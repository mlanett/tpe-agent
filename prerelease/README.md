# Prerelease Application

This code uses the ThreadPoolExecutor agent to monitor thread pools in a Java application.

However, in order to work before publishing, it depends on the other modules.
Thus, it indicates that the code will *probably* work after publishing, but can't guarantee it.

This is an internal module, not an example project.
